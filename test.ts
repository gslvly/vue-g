import { baseParse, baseCompile } from "@vue/compiler-core";
import * as fs from "fs";
// const a = fs.readFileSync("./src/create-render/normal-node.vue", "utf-8");
// console.log(
//   baseCompile(a, {
//     isCustomElement: (tag) => {
//       console.log({tag});
//       if (tag === "g-dom") return false;
//       if (tag.startsWith("g-")) return true;
//     },
//   })
// );
